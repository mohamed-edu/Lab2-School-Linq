@model IEnumerable<Lab2_School_Linq.Models.CourseClass>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CourseClassName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CourseClassName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CourseClassId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CourseClassId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CourseClassId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
